_format_version: "3.0"
_transform: true

services:
  - name: users-service
    url: http://host.docker.internal:8080/api/v1/users/
    routes:
      - name: users-route
        methods:
          - GET
        paths:
          - /users
